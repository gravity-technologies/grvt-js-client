import { type SchemaPairMap } from './types'

// Schema map for the 'API_GET_LP_CONFIG_REQUEST' struct.
export const API_GET_LP_CONFIG_REQUEST_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
    kind: 'k',
    base: 'b'
  },
  LITE_TO_FULL: {
    k: 'kind',
    b: 'base'
  }
})
