import { type SchemaPairMap } from './types'

// Schema map for the 'EMPTY_REQUEST' struct.
export const EMPTY_REQUEST_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
  },
  LITE_TO_FULL: {
  }
})
