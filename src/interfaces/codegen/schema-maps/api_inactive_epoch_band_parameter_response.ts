import { type SchemaPairMap } from './types'

// Schema map for the 'API_INACTIVE_EPOCH_BAND_PARAMETER_RESPONSE' struct.
export const API_INACTIVE_EPOCH_BAND_PARAMETER_RESPONSE_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
    ids: 'i'
  },
  LITE_TO_FULL: {
    i: 'ids'
  }
})
