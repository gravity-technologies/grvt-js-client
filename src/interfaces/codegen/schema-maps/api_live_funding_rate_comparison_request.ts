import { type SchemaPairMap } from './types'

// Schema map for the 'API_LIVE_FUNDING_RATE_COMPARISON_REQUEST' struct.
export const API_LIVE_FUNDING_RATE_COMPARISON_REQUEST_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
    agg_type: 'at'
  },
  LITE_TO_FULL: {
    at: 'agg_type'
  }
})
