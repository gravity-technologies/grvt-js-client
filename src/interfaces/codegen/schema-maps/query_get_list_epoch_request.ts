import { type SchemaPairMap } from './types'

// Schema map for the 'QUERY_GET_LIST_EPOCH_REQUEST' struct.
export const QUERY_GET_LIST_EPOCH_REQUEST_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
    limit: 'l'
  },
  LITE_TO_FULL: {
    l: 'limit'
  }
})
