import { type SchemaPairMap } from './types'

// Schema map for the 'API_SET_INITIAL_LEVERAGE_RESPONSE' struct.
export const API_SET_INITIAL_LEVERAGE_RESPONSE_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
    success: 's'
  },
  LITE_TO_FULL: {
    s: 'success'
  }
})
