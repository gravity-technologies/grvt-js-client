import { type SchemaPairMap } from './types'

// Schema map for the 'INTERNAL_PRE_ORDER_MARGIN_CHECK_RESPONSE' struct.
export const INTERNAL_PRE_ORDER_MARGIN_CHECK_RESPONSE_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
    result: 'r'
  },
  LITE_TO_FULL: {
    r: 'result'
  }
})
