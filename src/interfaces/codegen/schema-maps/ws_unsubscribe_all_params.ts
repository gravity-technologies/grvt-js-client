import { type SchemaPairMap } from './types'

// Schema map for the 'WS_UNSUBSCRIBE_ALL_PARAMS' struct.
export const WS_UNSUBSCRIBE_ALL_PARAMS_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
  },
  LITE_TO_FULL: {
  }
})
