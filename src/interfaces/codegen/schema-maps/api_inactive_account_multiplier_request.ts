import { type SchemaPairMap } from './types'

// Schema map for the 'API_INACTIVE_ACCOUNT_MULTIPLIER_REQUEST' struct.
export const API_INACTIVE_ACCOUNT_MULTIPLIER_REQUEST_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
    ids: 'i'
  },
  LITE_TO_FULL: {
    i: 'ids'
  }
})
