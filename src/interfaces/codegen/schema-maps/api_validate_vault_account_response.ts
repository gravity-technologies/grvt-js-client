import { type SchemaPairMap } from './types'

// Schema map for the 'API_VALIDATE_VAULT_ACCOUNT_RESPONSE' struct.
export const API_VALIDATE_VAULT_ACCOUNT_RESPONSE_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
    result: 'r'
  },
  LITE_TO_FULL: {
    r: 'result'
  }
})
