import { type SchemaPairMap } from './types'

// Schema map for the 'API_QUERY_VAULT_INVESTOR_HISTORY_STATS_REQUEST' struct.
export const API_QUERY_VAULT_INVESTOR_HISTORY_STATS_REQUEST_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
  },
  LITE_TO_FULL: {
  }
})
