import { type SchemaPairMap } from './types'

// Schema map for the 'WS_LIST_STREAMS_PARAMS' struct.
export const WS_LIST_STREAMS_PARAMS_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
  },
  LITE_TO_FULL: {
  }
})
