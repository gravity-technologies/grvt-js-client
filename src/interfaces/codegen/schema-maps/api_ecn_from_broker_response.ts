import { type SchemaPairMap } from './types'

// Schema map for the 'API_ECN_FROM_BROKER_RESPONSE' struct.
export const API_ECN_FROM_BROKER_RESPONSE_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
    result: 'r'
  },
  LITE_TO_FULL: {
    r: 'result'
  }
})
