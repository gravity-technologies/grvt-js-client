import { type SchemaPairMap } from './types'

// Schema map for the 'WS_DEPOSIT_FEED_SELECTOR_V_1' struct.
export const WS_DEPOSIT_FEED_SELECTOR_V_1_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
    main_account_id: 'ma'
  },
  LITE_TO_FULL: {
    ma: 'main_account_id'
  }
})
