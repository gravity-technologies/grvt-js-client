import { type SchemaPairMap } from './types'

// Schema map for the 'API_GET_CURRENCY_REQUEST' struct.
export const API_GET_CURRENCY_REQUEST_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
  },
  LITE_TO_FULL: {
  }
})
