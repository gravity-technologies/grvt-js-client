import { type SchemaPairMap } from './types'

// Schema map for the 'API_CREATE_ACCOUNT_MULTIPLIER_RESPONSE' struct.
export const API_CREATE_ACCOUNT_MULTIPLIER_RESPONSE_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
    id: 'i'
  },
  LITE_TO_FULL: {
    i: 'id'
  }
})
