import { type SchemaPairMap } from './types'

// Schema map for the 'API_DEACTIVATE_EPOCH_POINT_BOOST_CONFIGURATION_RESPONSE' struct.
export const API_DEACTIVATE_EPOCH_POINT_BOOST_CONFIGURATION_RESPONSE_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
    id: 'i'
  },
  LITE_TO_FULL: {
    i: 'id'
  }
})
