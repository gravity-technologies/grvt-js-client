import { type SchemaPairMap } from './types'

// Schema map for the 'API_GET_EPOCH_POINT_DISTRIBUTION_CONFIGURATION_REQUEST' struct.
export const API_GET_EPOCH_POINT_DISTRIBUTION_CONFIGURATION_REQUEST_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
    epoch: 'e'
  },
  LITE_TO_FULL: {
    e: 'epoch'
  }
})
