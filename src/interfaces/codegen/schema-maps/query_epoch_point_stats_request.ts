import { type SchemaPairMap } from './types'

// Schema map for the 'QUERY_EPOCH_POINT_STATS_REQUEST' struct.
export const QUERY_EPOCH_POINT_STATS_REQUEST_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
    epoch: 'e'
  },
  LITE_TO_FULL: {
    e: 'epoch'
  }
})
