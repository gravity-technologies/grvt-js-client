import { type SchemaPairMap } from './types'

// Schema map for the 'API_ADD_ISOLATED_POSITION_MARGIN_RESPONSE' struct.
export const API_ADD_ISOLATED_POSITION_MARGIN_RESPONSE_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
    success: 's'
  },
  LITE_TO_FULL: {
    s: 'success'
  }
})
