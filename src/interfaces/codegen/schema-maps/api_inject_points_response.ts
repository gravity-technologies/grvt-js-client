import { type SchemaPairMap } from './types'

// Schema map for the 'API_INJECT_POINTS_RESPONSE' struct.
export const API_INJECT_POINTS_RESPONSE_MAP: SchemaPairMap = Object.freeze({
  FULL_TO_LITE: {
    success: 's'
  },
  LITE_TO_FULL: {
    s: 'success'
  }
})
